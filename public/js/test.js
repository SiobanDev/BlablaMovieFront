const errorTemplate = Handlebars.compile(window.querySelector('#error-template').innerHTML, { title: 'yoyo' });
const moviesTemplate = Handlebars.compile(window.querySelector('#error-template').innerHTML;
const historicalTemplate = Handlebars.compile($('#exchange-template').html());
const contactTemplate = Handlebars.compile($('#historical-template').html());